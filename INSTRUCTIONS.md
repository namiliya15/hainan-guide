# Обновление — ответ пользователя админу + цветные метки на карте

## 1. Пользователь может ответить на вопрос администратора

Раньше переписка была только в одну сторону. Теперь на странице "Мои предложения" под ответом администратора появляется поле для ответа — отправленный ответ виден и там же, и у тебя во "Входящих" (с пометкой "Ответ автора"). Это по-прежнему не бесконечный чат, а по одному полю с каждой стороны, которое можно перезаписывать — если нужно уточнить что-то ещё раз, переписываете поверх предыдущего.

**Новый файл:** `supabase/migrations/006_user_replies.sql` — **обязательно выполнить** в SQL Editor.

**Изменённые файлы:**
- `src/features/feedback/useFeedback.js` — `submitUserReply`, `markUserRepliesSeen`, счётчик `unreadUserReplyCount`.
- `src/pages/MySubmissionsPage.jsx` — форма ответа под сообщением администратора.
- `src/pages/AdminInboxPage.jsx` — показывает ответ пользователя, отмечает прочитанным при заходе на страницу.
- `src/App.jsx`, `src/components/Navbar.jsx` — счётчик "Входящие" у админа теперь = новые заявки + новые ответы пользователей.

## 2. Цветные метки на карте

- Обычная метка — фирменный цвет (лагуна), выбранная — коралловая.
- Метка становится коралловой и при переходе с карточки места по кнопке "На карте", и при тапе прямо по метке на карте.
- Возвращается в обычный цвет, когда попап закрывается (клик по карте в другом месте, повторный тап и т.д.).

Заодно поймала и попутно исправила баг: раньше кнопка "На карте" рисовала **второй маркер поверх уже существующего** для того же места (вместо простой подсветки) — теперь это просто смена цвета той же самой метки.

**Изменённый файл:** `src/features/map/MapSection.jsx`.

## Что сознательно не стали делать

По плану оставались смена карты на Amap и построение маршрутов внутри приложения — договорились их не делать: цветные метки закрывают практическую часть задачи, а маршруты — по-прежнему открываются через диплинк в Amap (кнопка на карточке места), как и раньше.

---



Раньше ответ на предложение/жалобу сохранялся в базе, но пользователь никак не узнавал о нём. Теперь:

- Колокольчик 🔔 в шапке (у обычных вошедших пользователей — рядом с "Войти"/аватаром на десктопе, и в мобильном меню как "Мои предложения") — с точкой, если есть непрочитанный ответ.
- Всплывающее уведомление "Вам написал администратор сайта 🙂" — появляется поверх страницы, когда есть непрочитанный ответ, со ссылкой на страницу с ответом.
- Статус "прочитано" сбрасывается на странице `/my-submissions` — как только пользователь туда заходит, точка и всплывашка исчезают.

## Новые файлы
- `supabase/migrations/005_reply_notifications.sql` — **обязательно выполнить** в SQL Editor. Добавляет поле "ответ прочитан" и разрешает автору заявки отмечать это у своих же записей.
- `src/components/ReplyToast.jsx` — всплывающее уведомление.

## Изменённые файлы
- `src/features/feedback/useFeedback.js` — загружает свои заявки при любой роли (не только для обычных пользователей), при ответе админа сбрасывает "прочитано", добавлена функция `markRepliesSeen`.
- `src/pages/MySubmissionsPage.jsx` — отмечает ответы прочитанными при заходе на страницу.
- `src/components/Navbar.jsx` — колокольчик с точкой; заодно поправила связанный нюанс — ссылка "Входящие" у админа раньше пропадала на всех страницах, кроме главной (это было побочным следствием старого способа прятать кнопку "Добавить место" вне главной).
- `src/App.jsx` — единая точка вызова `useFeedback`, рендер всплывающего уведомления.

---



## Новые файлы

- `supabase/migrations/004_feedback.sql` — **обязательно выполнить** в Supabase SQL Editor. Создаёт таблицы `place_suggestions` и `place_reports` + права доступа.
- `src/features/feedback/useFeedback.js` — вся логика: отправка, чтение своих заявок, админ-функции (список, одобрение, отклонение, ответ).
- `src/features/feedback/SuggestPlaceForm.jsx` — форма "Предложить место" (кнопка под картой на главной).
- `src/features/feedback/ReportIssueForm.jsx` — форма "Сообщить об изменениях" (маленькая кнопка на каждой карточке места и в карточке по двойному тапу на карте).
- `src/pages/AdminInboxPage.jsx` — `/admin/inbox`, входящие для админа: список предложений и жалоб, кнопки "Опубликовать"/"Отклонить"/"Решено", поле ответа.
- `src/pages/MySubmissionsPage.jsx` — `/my-submissions`, обычный пользователь видит статус своих заявок и ответ админа (доступно только если вошёл в аккаунт).

## Изменённые файлы

- `src/features/places/PlaceCard.jsx`, `PlaceModal.jsx` — добавлена кнопка "Сообщить об изменениях".
- `src/pages/HomePage.jsx` — кнопка "Предложить место" под картой, подключены обе формы.
- `src/components/Navbar.jsx` — у админа появилась ссылка "Входящие" со счётчиком новых заявок (в шапке на десктопе и в мобильном меню); у обычных вошедших пользователей — "Мои предложения" в мобильном меню.
- `src/App.jsx` — новые маршруты `/admin/inbox`, `/my-submissions`.

## Как это работает

- Одобрение предложения места в "Входящих" **сразу публикует его** как обычное место на сайте (переносит все поля в таблицу `places`).
- Отправить и предложение, и жалобу может кто угодно, даже без входа — тогда после отправки формы просят email, чтобы админ мог ответить.
- Переписка — не полноценный чат, а одно поле "Ответ администратора" на каждую заявку (можно перезаписывать). Для простой связи "вопрос → ответ" этого достаточно; я сознательно не стала городить полноценный тред сообщений — это отдельная фича на будущее, если понадобится.
- У анонимных отправителей (без входа) нет способа посмотреть ответ в приложении — только через email, который они укажут. Если хочешь, чтобы они тоже видели статус — единственный надёжный вариант это сделать через вход в аккаунт.

---



## Новые файлы

- `supabase/migrations/003_articles.sql` — **обязательно выполнить** в Supabase SQL Editor (создаёт таблицу `articles` и права доступа: читать могут все опубликованные статьи, писать — только твой админский аккаунт).
- `src/features/articles/useArticles.js` — вся логика: список, создание, редактирование, публикация/черновик, удаление, загрузка обложки.
- `src/features/articles/RichTextEditor.jsx` — простой редактор текста (жирный/курсив/подчёркнутый/заголовок/список/ссылка/фото).
- `src/features/articles/ArticleCard.jsx` — карточка статьи для списка.
- `src/pages/ArticlePage.jsx` — страница одной статьи.
- `src/pages/ArticleEditorPage.jsx` — создание/редактирование статьи (доступно только тебе).

## Изменённые файлы

- `src/pages/ArticlesPage.jsx` — теперь настоящий список статей вместо заглушки, у админа есть кнопка "Написать статью".
- `src/App.jsx` — добавлены маршруты `/articles/new`, `/articles/:slug`, `/articles/:slug/edit`.

## Как это работает

- Статья может быть черновиком или опубликованной — черновики видишь только ты (проверяется на уровне базы данных, не только в интерфейсе).
- Редактор — на технологии попроще (`contentEditable`), а не на тяжёлой библиотеке типа Tiptap: делает то же самое (жирный, курсив, заголовки, списки, ссылки, фото), но без риска несовместимости пакетов, которую я не могу проверить сборкой у себя.
- Фото в статьях загружаются в тот же бакет Supabase Storage, что и фото мест (`place-images`), просто в отдельную папку `article-covers/`.
- Обложка статьи — отдельно от фото внутри текста, показывается в карточке и на шапке страницы статьи.

## Важно

Редактор на `contentEditable` — это упрощённое решение. Он не имеет built-in защиты от вредоносного HTML, но так как писать в него может только твой админский аккаунт (это проверяется в базе, не только в интерфейсе), риска для обычных пользователей нет.

---



Обе проблемы оказались одной и той же CSS-особенностью в разных местах: у карточки места есть эффект подъёма при наведении (`transform`), а у шапки — размытие фона (`backdrop-blur`, это `filter`). По стандарту CSS оба свойства заставляют браузер считать такой элемент "точкой отсчёта" для всех вложенных элементов с `position: fixed` — из-за этого просмотр фото и мобильное меню растягивались не на весь экран, а только в границах карточки/шапки.

**Исправлено:** оба слоя (`Lightbox.jsx` и мобильное меню в `Navbar.jsx`) теперь рендерятся через `createPortal` сразу в `<body>`, минуя эту проблему.

Заодно: под фото в полноэкранном просмотре добавлена серая подпись с названием места.

Изменились файлы:
- `src/components/Lightbox.jsx`
- `src/components/Navbar.jsx`
- `src/features/places/ImageCarousel.jsx`
- `src/features/places/PlaceCard.jsx`
- `src/features/places/PlaceModal.jsx`

---



Это добавка поверх того, что ты уже залила. Изменились/добавились только эти файлы (архив содержит полный проект целиком, но если заливаешь через GitHub Desktop — можешь просто перезаписать все файлы, лишним это не будет):

- `src/components/Navbar.jsx` — **изменён**: на экранах меньше `sm` (640px) вместо ссылок в строке теперь иконка "≡" справа, открывает выезжающее меню со всем содержимым шапки + дублирующим списком категорий.
- `src/components/Lightbox.jsx` — **новый**: полноэкранный просмотр фото с перелистыванием и закрытием по Esc/крестику/клику на фон.
- `src/features/places/ImageCarousel.jsx` — **изменён**: клик по фото открывает Lightbox; добавлен `variant="modal"`, при котором высота фото подстраивается под сам снимок (не обрезается фиксированной высотой).
- `src/features/places/PlaceModal.jsx` — **изменён**: карточка по двойному тапу на карте стала шире и использует адаптивный вариант карусели.
- `src/pages/HomePage.jsx` — **изменён**: умеет открывать нужную категорию по ссылке вида `/?category=Пляжи` — это то, на что ведут пункты в мобильном меню.

**Важная оговорка про мобильное меню категорий:** список там сейчас в изначальном порядке (`data/categories.js`), а не в твоём персональном порядке (drag&drop, который хранится в Supabase). Синхронизировать их можно, но это отдельная небольшая доработка — скажи, если это важно, добавлю.

---



## 1. Код разбит по папкам

Было — всё в одном `src/App.jsx` (1281 строка).
Стало:

```
src/
  App.jsx              # только роутинг, шапка, модалка входа
  main.jsx             # точка входа + роутер
  styles.css
  hooks/
    useSession.js       # сессия Supabase (вход/выход/подписка)
    useIsAdmin.js         # права админа — ОДНА константа email вместо 3 копий
    useTheme.js            # тёмная/светлая тема
  features/
    auth/
      AuthModal.jsx        # модалка входа/регистрации
    places/
      usePlaces.js           # вся логика мест: загрузка, добавление, поиск, фото
      PlaceCard.jsx            # карточка места в сетке (новый дизайн)
      PlaceModal.jsx             # карточка места по двойному тапу на карте
      ImageCarousel.jsx
      AddPlaceForm.jsx
    categories/
      useCategoryOrder.js
      CategoryMenu.jsx        # горизонтальные "пилюли" вместо бокового меню
      SortableCategoryPill.jsx
    favorites/
      useFavorites.js         # избранное + запрос авторизации для гостей
    map/
      MapSection.jsx           # карта: тап/двойной тап/геолокация
      useGeolocation.js
  pages/
    HomePage.jsx           # новая публичная главная
    ArticlesPage.jsx         # заглушка "Статьи скоро появятся"
  components/
    Navbar.jsx
    ThemeToggle.jsx
  data/                 # без изменений
  lib/                  # без изменений
```

## 2. Новая логика приложения

- Главная страница **публичная** — можно смотреть места, искать, пользоваться картой без входа.
- Вход нужен только для избранного (при попытке без входа — окно с просьбой войти) и админ-функций.
- Роутинг: `/` — главная, `/articles` — заглушка страницы статей.
- Дизайн из макета: hero, линия берега, плавающий поиск, горизонтальные категории, карточки одинаковой высоты с раскрытием текста, тёмная тема.
- Поиск ищет по всем местам сразу, независимо от категории.

## 3. Расширенное описание места (Фаза 3)

У места теперь три отдельных поля вместо одного общего описания:
- **Время работы** (`working_hours`) — показывается иконкой 🕐 на карточке
- **Стоимость** (`price_info`)
- **Описание** (`description`) — как раньше
- **Дополнительно** (`extra_info`) — любые доп. детали (как добраться, лайфхаки)

Заполняются в форме добавления/редактирования (доступна только админу).

**⚠️ Важно — нужно применить SQL-миграцию.** При изучении схемы БД обнаружила несостыковку: в `supabase/schema.sql` поля `photo_url`, `lat`, `lng` были помечены `NOT NULL`, а код приложения (и старый, и новый) пишет в поле `photos` (которого не было в таблице) и допускает место без координат. Это могло вызывать ошибку при сохранении места без фото по URL/без координат.

**Как применить:** зайди в Supabase Dashboard → SQL Editor → открой файл `supabase/migrations/002_place_details_and_fixes.sql` из архива → вставь содержимое → Run. Это добавит новые поля и уберёт лишние NOT NULL. Безопасно запускать даже если что-то из этого уже было исправлено вручную раньше.

## 4. Карта (Фаза 4)

- Одиночный тап по метке — всплывает название места (стандартный Leaflet Popup).
- Двойной тап — открывается карточка места (`PlaceModal`) поверх карты: тёмный фон, крестик в углу для закрытия, без скролла к карточке в списке.
- Кнопка "Моё местоположение" — по запросу (не автоматически) показывает на карте, где находится пользователь.
- Для админа сохранено прежнее поведение: клик по карте добавляет точку, маркеры перетаскиваются.

**Не включено:** замена карты на Amap/другой провайдер и построение маршрута — см. план, это отдельный пункт (можно сделать быстро через диплинки, но нужно отдельно проверить актуальную схему ссылок).

## 5. Что осталось (следующий шаг)

- Блог/статьи с редактором для админа
- Формы обратной связи от пользователей (предложить место / сообщить об изменениях) + инбокс админа
- Смена карты на Amap + построение маршрута через диплинк

Это самые объёмные куски — пишу их следующим шагом.

## 6. Как залить на GitHub (без терминала)

1. Открой репозиторий `hainan-guide` на GitHub.
2. Удали старую папку `src` целиком.
3. **Add file → Upload files** — перетащи папку `src` из архива (структура сохранится).
4. Так же замени: `package.json`, `tailwind.config.js`, `index.html`, `supabase/schema.sql`.
5. Добавь новый файл `supabase/migrations/002_place_details_and_fixes.sql` — и обязательно выполни его в Supabase SQL Editor (см. пункт 3 выше — это не применится само по себе просто от заливки на GitHub).
6. Остальное (`vite.config.js`, `postcss.config.js`, `.env.example`, `.gitignore`, `vercel.json`, `public/`, `DEPLOYMENT.md`) не менялось.
7. Commit changes — Vercel пересоберёт сайт сам.

## 7. Если сборка упадёт

Пришли мне текст ошибки из Vercel — разберём вместе. Учти: я не могу прогнать `npm install` и `vite build` у себя, чтобы проверить это заранее — сеть здесь недоступна, так что синтаксис я проверила вручную, но не 100% гарантирую сборку с первого раза.

